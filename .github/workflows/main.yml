name: Update badges

on:
  schedule:
    # Runs every minute
    - cron: "* * * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
        with:       
          CREDLY_USER: khaled-naes # optional, but default will use the same from github
